{
  "permissions": {
    "allow": [
      "Bash(cargo build:*)",
      "Bash(cargo test:*)",
      "Bash(powershell -Command \"Measure-Command { cargo run --release --bin folio-mcp 2>&1 | Out-Null }\")",
      "Bash(cargo run:*)"
    ]
  }
}
