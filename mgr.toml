# MGR workload manifest (v1). [app] moved to sigiled.toml (v2 cutover,
# same handoff as reddit-mine and tomes-and-tales): the v2 engine reads
# sigiled.toml first, and removing it here deregisters the app from v1
# (container left as-is — no downtime). v1 keeps driving sessions from
# this file until the switch.
