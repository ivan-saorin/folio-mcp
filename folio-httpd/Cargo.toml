[package]
name = "folio-httpd"
description = "HTTP JSON API for Folio — searxng-style resident service for the automa stack"
version.workspace = true
edition.workspace = true
license.workspace = true

[[bin]]
name = "folio-httpd"
path = "src/main.rs"

[dependencies]
folio = { path = "../folio" }
folio-mcp = { path = "../folio-mcp" }
axum = "0.8"
tokio = { workspace = true }
serde_json = { workspace = true }
